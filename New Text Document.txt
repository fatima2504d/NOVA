CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    password VARCHAR(255),
    address TEXT,
    cell_phone VARCHAR(20),
);

orders
user_id, order_id, products_id, price, customer_name, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

product
product_id, product_name, company ,price, metaluse

CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    category VARCHAR(50),
    price DECIMAL(10,2),
    image VARCHAR(255),
    brand,
    metal,
    size
);

CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(100),
    phone VARCHAR(20),
    address TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE order_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    product_id INT,
    quantity INT
);

CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_number VARCHAR(100),
    user_id INT,
    total_amount DECIMAL(10,2),
    status VARCHAR(50),
    payment_method VARCHAR(50),
    address TEXT,
    city VARCHAR(100),
    postal_code VARCHAR(20),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

ADD IMAGE IN PRODUCTS

SQL
(ALTER TABLE product ADD image VARCHAR(255);

UPDATE product SET image='ring.jpg' WHERE product_id=1;)

PHP
(<img src="images/<?php echo $row['image']; ?>" class="card-img-top" height="200">